# Implementation Plan

<!-- Copy this template into the project's temp/implementation-plan.md at the start of each Code Implementation cycle. Keep the file until every item's checkboxes are checked, and do not delete it while any item remains unfinished. -->

## Items

- [ ] `<ID>` — `<Title>` (`#N`) — branch: `<prefix>/<desc>`

  - [ ] Plan reviewed and fixed
  - [ ] Implemented on branch
  - [ ] Tracker and CHANGELOG updated
  - [ ] PR opened with `--body-file`
  - [ ] Merged

  Dependencies: `<none | waits on #N>`

## Dependency order

`<which items must follow which, so a dependent item starts only after its dependency has merged>`

## Notes

`<anything else relevant to the planned implementation>`